[project]
name = "06-own_agent"
version = "0.1.0"
description = "カスタマイズ用プロダクション品質ベースライン ReAct エージェント"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.0.0",
    "llama-index-core>=0.13.0",
    "llama-index-llms-google-genai>=0.1.0",
    "nest-asyncio>=1.6.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.8.0",
]

[tool.uv]
exclude-newer = "3 days"
